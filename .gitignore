_site/
.jekyll-cache/
.jekyll-metadata
vendor/
.bundle/
.sass-cache/

# 내부 저작 가이드·컨텍스트 (로컬 전용)
# ⚠️ 백업 대상이다. clone하면 따라오지 않는다.
CLAUDE.md
_workspace/

# ⚠️ `_instructions/` 는 추적하지 않는다 (2026-09-07, 퍼블릭 공개 결정).
#    정답지·강사용 산출물·저작 계획이 들어 있어 수강생에게 보이면 안 된다.
#    ★ 로컬 전용이므로 백업 대상이다. clone 하면 따라오지 않는다 —
#      재료 생성기·검증기가 그 안에 있어 잃으면 재료를 다시 만들 수 없다.
_instructions/

# Office 잠금 잔여물 (업무 파일이 아니다)
~$*
